image_speed = 0;
image_index = irandom_range(0, 2);

image_angle = random(360);
gravity = 0;
direction = irandom(360);
speed = irandom_range(2, 12) * 0.1;
image_alpha = irandom_range(4, 8) * 0.1;
angspd = random_range(2,8);